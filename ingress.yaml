apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: day1-ingress
spec:
  rules:
  - host: myapp.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: day1-service
            port:
              number: 80
